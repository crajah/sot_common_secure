package parallelai.common.secure.diffiehellman

import org.scalatest._

class DiffieHellmanTest extends FlatSpec with Matchers with DiffieHellmanClient with DiffieHellmanServer {
  /*"Diffe-Hellman Client and Server shared secret" should "be the same" in {
    // Begin Key Exchange
    val clientKey = clientPublicKey
    val serverKey = serverPublicKey(clientKey)
    val clientSecret = clientSharedSecret(serverKey)

    // TODO
    /*val cs = getClientSharedSecret
    val ss = getClientSharedSecret

    cs shouldEqual ss*/
  }*/
}